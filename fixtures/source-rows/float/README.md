# Float Source Rows

Use this folder for redacted Float raw task, allocation, cache, archive, inactive,
multi-person split, raw-without-cache, and cache-without-raw cases.

Rows must preserve raw task identity, expanded allocation identity when present,
cache identity when present, active/archive state, and the expansion rule used by
later parsed facts.
