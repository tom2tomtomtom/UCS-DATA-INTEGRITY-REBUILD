import { describe, test } from "vitest";

describe("Identity matching policy", () => {
  test.todo("uses fee-sheet Float project ID as canonical join evidence when present");
  test.todo("keeps source client and canonical client distinguishable");
  test.todo("preserves TBC row identity without collapsing unrelated rows");
  test.todo("preserves no-job row evidence or records an allowed skipped-row reason");
});
