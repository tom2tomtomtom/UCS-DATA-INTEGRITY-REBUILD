import { describe, test } from "vitest";

describe("Law 10: approval requires source, contract, UI, export, and chat agreement", () => {
  test.todo("requires unit evidence before approval");
  test.todo("requires display contract evidence before approval");
  test.todo("requires real-data verify evidence before approval");
  test.todo("requires deterministic UI evidence before approval");
  test.todo("requires CSV parity evidence before approval");
  test.todo("requires chat evidence parity when chat can answer the question");
});
