import { describe, test } from "vitest";

describe("Law 9: chat is read-only evidence reporting", () => {
  test.todo("does not answer from a bad pasted transcript as product truth");
  test.todo("guards against USA sold-hours false-zero claims");
  test.todo("reports PCS stale-cache warnings instead of unsupported certainty");
  test.todo("turns tool errors into visible evidence warnings");
  test.todo("requires serial evidence checks before final claims");
  test.todo("hands off with Needs Codex when code, browser, mutation, deploy, or stakeholder work is required");
});
