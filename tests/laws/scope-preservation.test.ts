import { describe, test } from "vitest";

describe("Law 4: scope is explicit and preserved", () => {
  test.todo("preserves office, date range, and department from rollup to Projects");
  test.todo("preserves all active scope from Projects to project detail");
  test.todo("uses exact client scope for client drilldown instead of fuzzy search");
  test.todo("keeps Q1 and full-year scopes as separate evidence contexts");
  test.todo("preserves Float project ID through scoped drilldowns");
});
