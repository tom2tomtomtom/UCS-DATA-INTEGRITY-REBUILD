import { describe, test } from "vitest";

describe("Law 8: archive state is a warning, not a hide rule", () => {
  test.todo("keeps archived production revenue visible in Projects and CSV");
  test.todo("keeps archived Float projects with hours visible in Float diagnostics");
  test.todo("keeps archived duplicates visible as mismatch evidence");
});
