import { describe, test } from "vitest";

describe("Tolerance, units, and time policy", () => {
  test.todo("keeps USD fee sheet values labelled with currency evidence");
  test.todo("uses explicit FX snapshot evidence when currency conversion is supported");
  test.todo("keeps Q1 boundary dates scoped exactly");
  test.todo("applies rounded-hour tolerance only where the policy permits it");
});
