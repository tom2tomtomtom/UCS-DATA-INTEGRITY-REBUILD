import { describe, test } from "vitest";

describe("Mutation boundary", () => {
  test.todo("keeps chat read-only");
  test.todo("keeps integrity checks read-only");
  test.todo("treats archive state as an overlay rather than a source mutation");
  test.todo("keeps export compare read-only");
});
